import React from 'react';
import PropTypes from 'prop-types';

const Details = props => {

    return (
        <div>
            detail
        </div>
    );
};

Details.propTypes = {

};

export default Details;